spring:
  data:
    redis:
      database: 0
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Abc12378(
  flyway:
    baseline-on-migrate: true
logging:
  level:
    com.apzda: debug
    sql: debug
